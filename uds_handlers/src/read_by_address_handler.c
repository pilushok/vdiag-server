#include <stdint.h>
#include <uds.h>

static int handle_read_data_by_address(const uint8_t *request,
                                       uint8_t *response,
                                       struct uds_state *puds) {
    return -1;
}
